[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py wait_for_db --timeout 120 && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn agricultura_inteligente.wsgi:application --bind 0.0.0.0:$PORT"
